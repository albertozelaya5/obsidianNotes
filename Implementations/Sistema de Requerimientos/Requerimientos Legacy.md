```tsx
 {/* 
          <StyledSelectBox>
            <SelectBox
              data={statuses.map((status: RequirementStatusResponse) => ({
                value: status.requirementStatusId,
                description: status.requirementStatusName,
              }))}
              control={control}
              errors={errors}
              valueExpr="value"
              displayExpr="description"
              name="unsolvedStatus"
              label="Estado Sin Resolver"
              showClearButton={true}
              searchEnabled={true}
            />
          </StyledSelectBox> */}
          
          {/* <SelectBox
              data={statuses.map((status: RequirementStatusResponse) => ({
                value: status.requirementStatusId,
                description: status.requirementStatusName,
              }))}
              control={control}
              errors={errors}
              valueExpr="value"
              displayExpr="description"
              name="solvedStatus"
              label="Estado Resuelto"
              showClearButton={true}
              searchEnabled={true}
            /> */}
```