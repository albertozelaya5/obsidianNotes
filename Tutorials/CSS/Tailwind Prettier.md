
```bash
npm install -D prettier prettier-plugin-tailwindcss
```

Lo que hace este paquete es ordenar las clases de la forma en que tailwind nos recomienda, nos ayuda porque siempre encontraremos las mismas classNames en la misma posición

`// .prettierrc`

```js
{
"plugins": ["prettier-plugin-tailwindcss"]
}
```
